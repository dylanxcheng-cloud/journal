{
  "name": "daybook",
  "version": "1.0.0",
  "description": "Daily habits, a journal, and the things people ask you to do \u2014 with reminders as notifications, a wallpaper, or a widget.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 8000",
    "build": "node tools/build-www.mjs",
    "ios:sync": "npm run build && npx cap sync ios && npm run ios:configure",
    "ios:open": "npx cap open ios",
    "ios:configure": "cd ios/App && ruby ../../tools/ios-configure.rb",
    "android:sync": "npm run build && npx cap sync android",
    "android:open": "npx cap open android",
    "android:apk": "npm run android:sync && cd android && ./gradlew assembleDebug"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.9",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/filesystem": "^7.1.8",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/local-notifications": "^7.0.0",
    "@capacitor/share": "^7.0.4"
  }
}
